# DocGen2 Editor Fast Transfer Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace the current project preview panel with the DocGen2 editor area.

**Architecture:** Keep the existing Formatta/layout-agent shell, left sources/results panel, and chat panel. Replace only `projects/work_panel.html` with a DocGen2-shaped editor card and add a small local JavaScript controller for basic toolbar behavior.

**Tech Stack:** FastAPI/Jinja templates, local CSS generated by Tailwind CLI, local vanilla JavaScript, existing HTMX.

## Global Constraints

- Do not read or print `.env`.
- Do not add React/Tiptap dependencies for this fast transfer.
- Use DocGen2 live UI and source files as the visual/functionality reference.
- No inline scripts, inline styles, or remote assets.

---

### Task 1: Editor panel markup and regression tests

**Files:**

- Modify: `app/tests/projects/test_routes.py`
- Modify: `app/tests/test_offline_ui.py`
- Modify: `app/src/docgen/templates/projects/work_panel.html`

**Interfaces:**

- Consumes: existing `document` and `project_id` template variables.
- Produces: `#docgen2Editor`, `.editor-card`, `.editor-topline`, `.toolbar`, `.editor-scroll`, `.document-canvas`.

- [ ] Write failing tests expecting the DocGen2 editor classes and no `#previewPanel`.
- [ ] Run focused tests and verify RED.
- [ ] Replace preview markup with DocGen2 editor markup.
- [ ] Run focused tests and verify GREEN.

### Task 2: Styling and toolbar behavior

**Files:**

- Modify: `app/src/docgen/static/css/tailwind-source.css`
- Modify: `app/src/docgen/static/css/docgen.css`
- Modify: `app/src/docgen/static/vendor/VERSIONS.md`
- Create: `app/src/docgen/static/js/docgen2-editor.js`
- Modify: `app/src/docgen/templates/base.html`
- Modify: `app/tests/test_offline_ui.py`
- Modify: `app/tests/test_package_install.py`

**Interfaces:**

- Consumes: editor markup from Task 1.
- Produces: local script `/static/js/docgen2-editor.js`; CSS SHA in `VERSIONS.md`.

- [ ] Write failing tests for local script inclusion and wheel packaging.
- [ ] Run focused tests and verify RED.
- [ ] Add DocGen2 editor CSS and JS.
- [ ] Regenerate CSS with `tailwindcss@3.4.17`.
- [ ] Update CSS SHA-256.
- [ ] Run focused tests and verify GREEN.

### Task 3: Browser verification

**Files:**

- No production file changes unless verification exposes a mismatch.

**Interfaces:**

- Consumes: local server on `127.0.0.1:8000` and DocGen2 reference on `127.0.0.1:5173`.
- Produces: visual evidence and final status.

- [ ] Open the local project detail page.
- [ ] Compare editor region dimensions and visible controls with DocGen2.
- [ ] Run `ruff check .`.
- [ ] Report exact test and browser verification results.
